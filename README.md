# media-sourcer
Next.js app + server that fetches YouTube and TikTok metadata (embeds videos, shows channel/uploader and source attribution). Metadata-only; uses yt-dlp server-side. Starter frontend with video-style UI.
